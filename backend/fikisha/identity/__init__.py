"""Identity & Access — the custom User, phone+OTP authentication, opaque
server-side sessions with refresh rotation, admin role assignment, and the
server-side authorization policy engine (Phase 1 domain-architecture §3.1,
authentication-authorization.md).
"""
