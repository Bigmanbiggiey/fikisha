"""Operator Groups — the minimal group construct (D-OPR-GRP-1, Phase 1
domain-architecture §3.4): a group profile and its membership.

NOT a fleet-management system. No payroll, scheduling, dispatch, maintenance,
route planning, inter-group transfers, or group-owned vehicles (vehicles arrive
in a later phase). The group exists to represent a real transport organisation
and to coordinate its members for future Jobs.
"""
