# Fikisha backend — base runtime dependencies (Phase 2A foundation).
# Pinned to compatible-release (~=) ranges within a minor line.
# Exact versions are captured in docs/phase-2/technology-versions.md after install.

Django~=5.2.0                 # LTS line — see phase-2a-decisions.md ADR-2A-01
djangorestframework~=3.16.0
psycopg[binary]~=3.2.0        # PostgreSQL driver (psycopg 3)
django-cors-headers~=4.6.0
celery~=5.4.0
redis~=5.2.0
structlog~=24.4.0
gunicorn~=23.0.0
whitenoise~=6.8.0
argon2-cffi~=23.1.0           # hashing for OTP codes, refresh tokens, recipient link tokens (later)
drf-spectacular~=0.28.0       # OpenAPI schema (foundation only)
